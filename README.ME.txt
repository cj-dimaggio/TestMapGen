A simple project in Haxe/openfl for experimenting procedural generation.
Includes a perlin noise generator and a simple random noise generator,
as well as a hexagon grid generator with cells that can be written to and drawn as if
pixels in a random noise bitmap.